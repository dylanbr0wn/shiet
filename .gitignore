# ── Wails / build output ──────────────────────────────────────────────
build/bin
frontend/dist

# ── Go ────────────────────────────────────────────────────────────────
# Test/coverage artifacts
*.test
*.out
coverage.*
# Compiled local binaries (e.g. `go build` in repo root)
/shiet
/db

# ── Frontend (Node / Vite) ────────────────────────────────────────────
node_modules
frontend/node_modules
frontend/.vite
*.tsbuildinfo
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store

# Buf-generated Protobuf / Connect clients and handlers
/gen/
/frontend/src/gen/

# TypeScript project-reference output from vite.config.ts
/frontend/vite.config.js
/frontend/vite.config.d.ts

# ── Local databases (SQLite + WAL/SHM sidecars) ───────────────────────
*.dev.db
*.dev.db-wal
*.dev.db-shm
*.db-wal
*.db-shm

# ── Secrets / local config ────────────────────────────────────────────
.env
.env.*
!.env.example
*.local

# ── Editor / OS ───────────────────────────────────────────────────────
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
*~
package-lock.json
